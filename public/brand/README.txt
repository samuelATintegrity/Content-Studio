Drop your logo here as logo.png (transparent background recommended).
The compose pipeline reads brand.config.ts which points at /brand/logo.png by default.
If no logo file is present, posts will render without a logo (no error).
